  python3 env.py 
  docker build -t flaskapp:3 .
  docker images
  docker network  create --driver bridge mynetwork 
  docker run -d --name flaskappp -network mynetwork -p 80:8000 flaskapp:3
  docker exec flaskappp ps aux
  docker exec flaskappp ls -la

